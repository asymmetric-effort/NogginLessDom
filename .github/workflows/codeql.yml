name: CodeQL Analysis

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: ['javascript-typescript']
    steps:
      - name: Checkout
        uses: asymmetric-effort/actions/actions/checkout@main
      - name: Initialize CodeQL
        uses: asymmetric-effort/actions/actions/codeql-init@main
        with:
          languages: ${{ matrix.language }}
      - name: Autobuild
        uses: asymmetric-effort/actions/actions/codeql-autobuild@main
      - name: Perform CodeQL Analysis
        uses: asymmetric-effort/actions/actions/codeql-analyze@main
        with:
          category: "/language:${{ matrix.language }}"
