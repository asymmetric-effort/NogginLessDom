{
  "name": "nogginlessdom-site",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "bun scripts/build-content.ts",
    "build": "vite build",
    "dev": "bun scripts/build-content.ts && vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "@asymmetric-effort/specifyjs": "^0.2.33"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
